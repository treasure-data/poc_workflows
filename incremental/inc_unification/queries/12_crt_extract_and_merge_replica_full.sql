---- canonical_id_source_key_stats_full is created to keep a log of all unification source key_stats in full mode even if unification is running incrementally.

----*******************
--  Below query is copied from first run from full unification from task +extract_and_merge
-- We have changed ONLY INPUT SOURCE TABLE which will have full data
----*******************

${sql_str_extract_and_merge}
